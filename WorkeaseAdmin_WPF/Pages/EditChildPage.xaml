<Page x:Class="WorkeaseAdmin_WPF.Pages.EditChildPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Edit Child">
    <Grid Background="#F5F0FF">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Background="{StaticResource DeepPurple}">
            <Button Content="Manage Children" Click="ManageChildren_Click" Background="{StaticResource DeepPurple}" Foreground="White" Height="50" BorderThickness="0"/>
            <Button Content="Edit Child" Background="{StaticResource SidebarSelection}" Foreground="White" Height="50" BorderThickness="0" Margin="0,2,0,0"/>
            <Button Content="Add Child" Click="AddChildren_Click" Background="#2ECC71" Foreground="White" Height="50" BorderThickness="0" Margin="0,2,0,0"/>
            <Button Content="Delete Child" Click="DeleteChild_Click" Background="#e12749" Foreground="White" Height="50" BorderThickness="0" Margin="0,2,0,0"/>
        </StackPanel>

        <Grid Grid.Column="1" Margin="25">
            <Border Background="White" CornerRadius="10" BorderBrush="{StaticResource PrimaryPink}" BorderThickness="1" VerticalAlignment="Top">
                <StackPanel>
                    <Border Background="{StaticResource PrimaryPink}" Height="55" CornerRadius="9,9,0,0">
                        <TextBlock Text="EDIT CHILD DETAILS" Foreground="White" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Margin="30">
                        <TextBlock Text="Search ID First" Foreground="Gray" Margin="0,0,0,5"/>
                        <TextBox Height="35" Margin="0,0,0,20"/>
                        <Button Content="Update Record" Width="150" Height="40" Background="{StaticResource CardBlue}" Foreground="White" HorizontalAlignment="Right" BorderThickness="0"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>