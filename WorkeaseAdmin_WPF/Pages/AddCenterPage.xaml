<Page x:Class="WorkeaseAdmin_WPF.Pages.AddCenterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Add Center">

    <Grid Margin="30">
        <Border Background="White" CornerRadius="10" Padding="30" Effect="{DynamicResource {x:Static SystemParameters.DropShadowKey}}">
            <StackPanel>
                <Border Background="{StaticResource PrimaryPink}" Margin="-30,-30,-30,20" CornerRadius="10,10,0,0" Height="50">
                    <TextBlock Text="Add Center" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Margin="10">
                        <TextBlock Text="Barangay" FontWeight="Bold" Margin="0,0,0,5"/>
                        <ComboBox Height="35" Text="- Select baranggay -"/>
                        <TextBlock Text="/* Selects barangay from the drawer" Foreground="#85E15F" FontSize="11" Margin="0,5,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="10">
                        <TextBlock Text="CDW worker" FontWeight="Bold" Margin="0,0,0,5"/>
                        <ComboBox Height="35" Text="- Select CDW Worker -"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Margin="10">
                        <TextBlock Text="Center name" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBox Style="{StaticResource ModernTextBox}"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="10">
                        <TextBlock Text="Contact person" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBox Style="{StaticResource ModernTextBox}"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="/* Add a bit of description about this page" Foreground="#85E15F" HorizontalAlignment="Center" Margin="0,50,0,0"/>

                <Button Content="Create New Center" Width="180" Height="40" HorizontalAlignment="Right" Margin="0,50,0,0"
                        Background="{StaticResource CardBlue}" Foreground="White" BorderThickness="0"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>