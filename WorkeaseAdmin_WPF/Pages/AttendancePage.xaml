<Page x:Class="WorkeaseAdmin_WPF.Pages.AttendancePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Attendance">

    <Grid Background="#F5F0FF">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar Navigation -->
        <StackPanel Grid.Column="0" Background="{StaticResource DeepPurple}">
            <Button Content="Attendance" Background="{StaticResource SidebarSelection}" Foreground="White" Height="50" BorderThickness="0" Cursor="Hand"/>
        </StackPanel>

        <Grid Grid.Column="1" Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Search Bar Section (Kopyang-kopya sa Edit Center style) -->
            <Border Grid.Row="0" Background="White" CornerRadius="10" Padding="20" VerticalAlignment="Top" Height="100" BorderBrush="#FF69B4" BorderThickness="2" Margin="0,0,0,20">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <StackPanel VerticalAlignment="Bottom">
                        <TextBlock Text="Search Child Name" Foreground="Gray" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox x:Name="SearchChildBox" Width="250" Height="35" VerticalContentAlignment="Center" Padding="10,0" BorderBrush="#DDD"/>
                    </StackPanel>
                    <Button Content="Search" Width="120" Height="35" Margin="20,0,0,0" VerticalAlignment="Bottom" Background="#38b6ff" Foreground="White" BorderThickness="0" FontWeight="Bold" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="1"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                </StackPanel>
            </Border>

            <!-- Attendance Table Section -->
            <Border Grid.Row="1" Background="White" CornerRadius="10" BorderBrush="#FF69B4" BorderThickness="1" ClipToBounds="True">
                <DockPanel>
                    <!-- Table Header -->
                    <Border DockPanel.Dock="Top" Background="#FF69B4" Height="60">
                        <TextBlock Text="ATTENDANCE MONITORING" Foreground="White" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <Grid>
                        <DataGrid AutoGenerateColumns="False" Background="Transparent" BorderThickness="0" 
                                  HeadersVisibility="Column" GridLinesVisibility="Horizontal" 
                                  RowHeight="40" AlternatingRowBackground="#FAF5FF">
                            <DataGrid.Resources>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#F3E5F5"/>
                                    <Setter Property="Foreground" Value="#7E57C2"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Padding" Value="10,5"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                    <Setter Property="BorderBrush" Value="#DDD"/>
                                </Style>
                            </DataGrid.Resources>

                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Child ID" Binding="{Binding ChildID}" Width="100"/>
                                <DataGridTextColumn Header="Full Name" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="Center" Binding="{Binding Center}" Width="*"/>
                                <DataGridTextColumn Header="Time In" Binding="{Binding TimeIn}" Width="120"/>
                                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- Empty State Message -->
                        <TextBlock Text="/* Attendance records will show here */" Foreground="#BDC3C7" FontSize="14" FontStyle="Italic" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,50,0,0"/>
                    </Grid>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</Page>