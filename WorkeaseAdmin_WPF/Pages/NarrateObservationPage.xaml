<Page x:Class="WorkeaseAdmin_WPF.Pages.NarrateObservationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WorkeaseAdmin_WPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Narrate Observation" Background="White">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Narrative Report Observation" FontSize="24" FontWeight="Bold" Foreground="#333"/>
            <TextBlock Text="Input your observations and findings for the monthly narrative report." 
                       FontSize="14" Foreground="#666" Margin="0,5,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Background="#F8F9FA" CornerRadius="8" Padding="15" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Target Center:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock x:Name="TxtCenterName" Text="No Center Selected" VerticalAlignment="Center" Foreground="#0078D7"/>
            </StackPanel>
        </Border>

        <DockPanel Grid.Row="2">
            <TextBlock DockPanel.Dock="Top" Text="Observations / Narrative Text" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="5">
                <TextBox x:Name="TxtObservation" 
                         AcceptsReturn="True" 
                         TextWrapping="Wrap" 
                         VerticalScrollBarVisibility="Auto" 
                         Padding="10"
                         FontSize="14"
                         BorderThickness="0"/>
            </Border>
        </DockPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancel" Width="100" Height="35" Margin="0,0,10,0" 
                    Background="Transparent" BorderBrush="#CCC" Click="Cancel_Click"/>
            
            <Button Content="Generate Word Report" Width="180" Height="35" 
                    Background="#0078D7" Foreground="White" FontWeight="Bold"
                    Click="GenerateNarrative_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            CornerRadius="5" 
                                            BorderThickness="0">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Page>